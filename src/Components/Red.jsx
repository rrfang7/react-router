export default function Red() {
    // const navigate = useNavigate();
    // setTimeout(() => navigate("/blue"), 1000)
    return (
        <div className="red">
            <h1>Red</h1>
        </div>
    );
}