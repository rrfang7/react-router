export default function Blue() {
    // const navigate = useNavigate();
    // setTimeout(() => navigate("/red"), 1000)

    return (
        <div className="blue">
            <h1>Blue</h1>
        </div>
    );
}